function calculateArea(width, height) {
    const x=width*height;
    console.log(`area of rectangle ${width}, ${height}! is ${x}`);

}
const width = 5;
const height = 3;
calculateArea(width, height);
